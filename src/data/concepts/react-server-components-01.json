{
  "id": "react-server-components-01",
  "slug": "react-server-components",
  "title": "React Server Components (RSC)",
  "domain": "Frontend",
  "category": "React & State",
  "difficulty": "advanced",
  "card": {
    "intuition": "A new architectural pattern where components can be rendered entirely on the server and sent to the browser as a specialized stream, reducing the amount of JavaScript sent to the client.",
    "analogy": "Ordering a pre-built Lego set: instead of receiving the instructions and all the pieces (Client Components), you receive the already-assembled 'Dragon' (Server Component). It takes up no space in your backpack (no JS bundle), but you can't change its pose (no interactivity) unless it has some 'interative joints' (Client Islands).",
    "time_complexity": "O(C) server components rendered",
    "space_complexity": "O(S) serialized state",
    "when_to_use": [
      "Data-heavy pages where you want minimal JS on the client (like a Blog or E-commerce list)",
      "Securely accessing databases or secret API keys directly from a component",
      "Reducing the 'Waterfall' of network requests in the browser"
    ],
    "gotchas": [
      "RSC CANNOT use hooks like `useState` or `useEffect` because they don't exist on the server",
      "They are NOT the same as Server-Side Rendering (SSR); SSR produces HTML, RSC produces a specialized binary format that React uses to update the UI"
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "steps": [
        {
          "label": "Request",
          "sub": "Server receives",
          "annotation": "Client navigates — server receives request."
        },
        {
          "label": "RSC render",
          "sub": "On server",
          "annotation": "Server Components render on the server: DB queries, no JS bundle shipped."
        },
        {
          "label": "Serialise",
          "sub": "RSC wire format",
          "annotation": "React serialises component tree (not HTML) to wire format."
        },
        {
          "label": "Client",
          "sub": "Merge tree",
          "annotation": "Client merges server tree with interactive Client Components."
        },
        {
          "label": "No hydration",
          "sub": "For SC",
          "annotation": "Server Components never hydrate — zero client-side JS for static parts."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the directive used at the top of a file to mark a component as interactive (client-side).",
    "starter_code": "# top of file\n'use ______'\n\nexport function Counter() { \n    const [count, setCount] = useState(0);\n    # ... \n}",
    "solution": "client",
    "hints": [
      "Where does it run? On the ______"
    ],
    "test_code": "def check(ans): return ans.lower() == \"client\"\nassert check(\"client\")\nprint(\"PASS\")"
  },
  "related": [
    "hydration-islands-01",
    "react-fiber-01",
    "bundlers-internals-01"
  ],
  "tags": [
    "react",
    "rsc",
    "server-side",
    "architecture",
    "performance",
    "frontend"
  ]
}