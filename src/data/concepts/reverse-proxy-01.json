{
  "id": "reverse-proxy-01",
  "slug": "reverse-proxy",
  "title": "Reverse Proxy",
  "domain": "Software Engineering",
  "category": "DevOps",
  "difficulty": "beginner",
  "card": {
    "intuition": "A server that sits in front of one or more backend applications and forwards client requests to them, hiding the identity and complexity of the internal systems.",
    "analogy": "A restaurant waiter: you talk to the waiter (Reverse Proxy). You don't know who is in the kitchen or how many chefs there are. The waiter takes your order, brings it to the kitchen, and brings you the food back.",
    "time_complexity": "O(1) overhead",
    "space_complexity": "N/A",
    "when_to_use": [
      "Adding a layer of security by hiding your real server IPs",
      "Performing SSL termination (handling HTTPS for an HTTP-only app)",
      "Caching content to reduce load on the backend"
    ],
    "gotchas": [
      "A Reverse Proxy sits in front of a SERVER. A 'Forward Proxy' sits in front of a CLIENT (used to bypass firewalls or browse anonymously). Don't mix them up!",
      "Header Spoofing: make sure your backend knows the 'True' IP of the client, not just the IP of the proxy (using `X-Forwarded-For`)."
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "mode": "proxy-request-forwarding",
      "show_header_translation": true,
      "show_internal_ip_hiding": true,
      "step_annotations": true
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the primary benefit of a reverse proxy for security.",
    "starter_code": "benefit = \"It hides the ________ IP address from the public internet.\"",
    "solution": "backend",
    "hints": [
      "Hides the servers in the 'back'"
    ],
    "test_code": "def check(ans): return \"back\" in ans.lower()\nassert check(\"backend\")\nprint(\"PASS\")"
  },
  "related": ["nginx-config-01", "load-balancer-internals-01", "ssl-termination-01"],
  "tags": ["proxy", "networking", "security", "devops", "nginx", "software-engineering"]
}
