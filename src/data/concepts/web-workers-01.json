{
  "id": "web-workers-01",
  "slug": "web-workers",
  "title": "Web Workers",
  "domain": "Frontend",
  "category": "Browser & Rendering",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A way to run JavaScript in a separate background thread, allowing you to perform heavy calculations without freezing the main UI thread.",
    "analogy": "A restaurant with a separate prep kitchen: the main chef (UI Thread) handles the plating and customers, while a 'Prep Cook' (Web Worker) chops 50kg of onions in the back room so the dining room doesn't slow down.",
    "time_complexity": "O(M) messages serialized",
    "space_complexity": "O(N) memory per worker",
    "when_to_use": [
      "Complex mathematical calculations",
      "Processing/filtering large amounts of data",
      "Image or audio processing in the browser"
    ],
    "gotchas": [
      "Web Workers have NO access to the DOM, `window`, or `document` objects",
      "Communication between the main thread and workers is done via an event-based system (`postMessage`) and data is cloned, not shared"
    ]
  },
  "visualization": {
    "type": "state-diagram",
    "config": {
      "mode": "worker-main-communication",
      "show_message_passing": true,
      "show_thread_isolation": true,
      "step_annotations": true
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Complete the logic for sending data to a worker and listening for a response.",
    "starter_code": "// Main Thread\nconst myWorker = new Worker('worker.js');\n\n// Send data\nmyWorker.______( [1, 2, 3] );\n\n// Receive data\nmyWorker.______( (e) => {\n    console.log('Result from worker:', e.data);\n});",
    "solution": "postMessage\nonmessage",
    "hints": [
      "You 'post' a message and then handle the 'on' message event"
    ],
    "test_code": "def check(pm, om): return pm == \"postMessage\" and om == \"onmessage\"\nassert check(\"postMessage\", \"onmessage\")\nprint(\"PASS\")"
  },
  "related": [
    "service-workers-01",
    "worker-threads-01",
    "event-loop-browser-01"
  ],
  "tags": [
    "web-worker",
    "concurrency",
    "performance",
    "multithreading",
    "frontend"
  ]
}