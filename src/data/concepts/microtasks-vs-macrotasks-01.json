{
  "id": "microtasks-vs-macrotasks-01",
  "slug": "microtasks-vs-macrotasks",
  "title": "Microtasks vs Macrotasks",
  "domain": "Frontend",
  "category": "JavaScript Core",
  "difficulty": "advanced",
  "card": {
    "intuition": "Two separate queues in the Event Loop: Microtasks (e.g., Promises) are higher priority and are all executed immediately after the current task finishes; Macrotasks (e.g., setTimeout) are executed one-by-one in subsequent loop cycles.",
    "analogy": "A VIP line at a club: The Macrotask queue is the regular line. Every time a person from the regular line enters (current task), the bouncer checks the 'VIP list' (Microtasks). ALL VIPs get to enter BEFORE the next person from the regular line is allowed in.",
    "time_complexity": "N/A",
    "space_complexity": "N/A",
    "when_to_use": [
      "Predicting the exact execution order of complex async code",
      "Clearing a microtask queue to avoid blocking the UI",
      "Using `queueMicrotask()` for internal framework state updates"
    ],
    "gotchas": [
      "A microtask that creates another microtask can literally block the event loop forever (starving the Macrotask queue and the renderer)",
      "Promises and `MutationObserver` use Microtasks; `setTimeout`, `setInterval`, and DOM events use Macrotasks"
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "mode": "queue-priority-execution",
      "show_microtask_exhaustion": true,
      "show_rendering_window": true,
      "step_annotations": true
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Predict the order of output (1, 2, 3).",
    "starter_code": "print('Start') # 1\n\n# Macrotask\nsetTimeout(lambda: print('Timeout'), 0) # ?\n\n# Microtask\nPromise.resolve().then(lambda: print('Promise')) # ?\n\nprint('End') # 2\n\n# Output order: 1, 2, ______, ______",
    "solution": "Promise\nTimeout",
    "hints": [
      "Microtasks (Promises) ALWAYS finish before the next Macrotask (Timeout) starts"
    ],
    "test_code": "def check(ans1, ans2): return ans1 == \"Promise\" and ans2 == \"Timeout\"\nassert check(\"Promise\", \"Timeout\")\nprint(\"PASS\")"
  },
  "related": ["event-loop-browser-01", "promises-internals-01", "async-await-js-01"],
  "tags": ["event-loop", "microtasks", "macrotasks", "async", "promises", "frontend"]
}
