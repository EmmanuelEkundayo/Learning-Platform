{
  "id": "server-sent-events-01",
  "slug": "server-sent-events",
  "title": "Server-Sent Events (SSE)",
  "domain": "Backend",
  "category": "Networking & HTTP",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A standard way for servers to push data to the browser over a single, long-lived HTTP connection — one-way only.",
    "analogy": "A radio station: the DJ (server) broadcasts music to your radio (browser) continuously, but you can't talk back through the radio.",
    "time_complexity": "O(1) per event push",
    "space_complexity": "O(C) open connections",
    "when_to_use": [
      "Progress bars for long-running server tasks",
      "Social media feeds / notifications where the user doesn't need to respond instantly",
      "Simpler alternative to WebSockets when you only need server-to-client updates"
    ],
    "gotchas": [
      "SSE is unidirectional (Server -> Client only)",
      "Browsers have a limit on the number of open SSE connections per domain (usually 6)"
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "steps": [
        {
          "label": "Client",
          "sub": "EventSource(url)",
          "annotation": "Client opens persistent HTTP connection via EventSource API."
        },
        {
          "label": "Server",
          "sub": "text/event-stream",
          "annotation": "Server responds with Content-Type: text/event-stream. Connection held open."
        },
        {
          "label": "Push",
          "sub": "data: payload\\n\\n",
          "annotation": "Server pushes data: ... events anytime. No polling needed."
        },
        {
          "label": "Reconnect",
          "sub": "Auto-retry",
          "annotation": "On disconnect, EventSource automatically reconnects with Last-Event-ID."
        },
        {
          "label": "One-way",
          "sub": "Server → Client",
          "annotation": "SSE is one-way: server → client only. Use WebSockets for bidirectional."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Fill in the required HTTP header and data format for SSE.",
    "starter_code": "# Server response headers\nheaders = {\n    \"Content-Type\": \"text/________-stream\",\n    \"Cache-Control\": \"no-cache\",\n    \"Connection\": \"keep-alive\"\n}\n\n# Message format\nmessage = \"____: My notification alert\\n\\n\"",
    "solution": "event\ndata",
    "hints": [
      "The content type for SSE is 'text/event-stream'",
      "Each message must start with 'data:' followed by the content"
    ],
    "test_code": "def check_sse(ctype, msg):\n    return \"event-stream\" in ctype and msg.startswith(\"data:\")\n\nassert check_sse(\"text/event-stream\", \"data: hello\")\nprint(\"PASS\")"
  },
  "related": [
    "websockets-01",
    "http-versions-01",
    "rest-principles-01"
  ],
  "tags": [
    "sse",
    "real-time",
    "push-notifications",
    "http",
    "backend"
  ]
}