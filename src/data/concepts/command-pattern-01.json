{
  "id": "command-pattern-01",
  "slug": "command-pattern",
  "title": "Command Pattern",
  "domain": "Software Engineering",
  "category": "Software Design",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A behavioral pattern that turns a request into a stand-alone object that contains all information about the request. This lets you pass requests as arguments, queue them, and support undoable operations.",
    "analogy": "A restaurant order ticket: a customer's request (e.g. 'One Burger') is written on a slip of paper (The Command). A waiter (Invoker) takes the paper and hands it to the chef (Receiver). Because the request is an object, it can be put in a queue, cancelled, or kept for accounting later.",
    "time_complexity": "O(1) per command execute",
    "space_complexity": "O(N) history for undo",
    "when_to_use": [
      "Implementing 'Undo' and 'Redo' functionality in an editor",
      "Queueing tasks to be executed by a background worker at a later time",
      "Decoupling the sender of a command (a button) from the receiver (the logic)"
    ],
    "gotchas": [
      "Boilerplate: for every small action, you have to create a new class. This can lead to a lot of small files.",
      "Command vs Strategy: they look similar! Strategy defines 'How' something is done; Command defines 'What' is to be done."
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "layers": [
        {
          "label": "Invoker",
          "items": [
            "Button",
            "Scheduler"
          ]
        },
        {
          "label": "Command",
          "items": [
            "execute()",
            "undo()"
          ]
        },
        {
          "label": "Receiver",
          "items": [
            "Editor",
            "DB",
            "API"
          ]
        }
      ],
      "steps": [
        {
          "active_layer": -1,
          "annotation": "System overview — step through to trace the flow."
        },
        {
          "active_layer": 0,
          "annotation": "Invoker holds a command object and calls execute() — no coupling to receiver."
        },
        {
          "active_layer": 1,
          "annotation": "Command object encapsulates the action, its params, and undo logic."
        },
        {
          "active_layer": 2,
          "annotation": "Receiver performs the actual work. Invoker never calls it directly."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the primary benefit of the Command pattern for user interfaces.",
    "starter_code": "benefit = \"It easily supports ____ and ____ functionality by keeping a history of objects.\"",
    "solution": "Undo\nRedo",
    "hints": [
      "Going backward and forward"
    ],
    "test_code": "assert \"undo\" in solution.lower()\nassert \"redo\" in solution.lower()\nprint(\"PASS\")"
  },
  "related": [
    "strategy-pattern-01",
    "observer-pattern-01",
    "factory-pattern-01"
  ],
  "tags": [
    "design-patterns",
    "behavioral",
    "command",
    "undo",
    "redo",
    "queues",
    "software-engineering"
  ]
}