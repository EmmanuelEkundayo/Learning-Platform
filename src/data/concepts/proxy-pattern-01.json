{
  "id": "proxy-pattern-01",
  "slug": "proxy-pattern",
  "title": "Proxy Pattern",
  "domain": "Software Engineering",
  "category": "Software Design",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A structural pattern that provides a surrogate or placeholder for another object to control access to it (e.g., for security, lazy loading, or logging).",
    "analogy": "A personal assistant: instead of talking to a celebrity (The Real Subject) directly, you talk to their assistant (The Proxy). The assistant can check your identification (Security), tell you the celebrity is busy (Lazy Loading), or record who you are (Logging) without ever bothering the celebrity.",
    "time_complexity": "O(1) overhead",
    "space_complexity": "O(1) — wraps single subject",
    "when_to_use": [
      "Lazy-loading giant images or database records only when they are actually needed",
      "Adding 'Access Control' to a sensitive service (e.g., checking if a user is an admin)",
      "Caching remote service call results to avoid redundant network traffic"
    ],
    "gotchas": [
      "The Proxy must implement the EXACT same interface as the real object so that the client doesn't even know it's talking to a proxy.",
      "Don't confuse it with 'Decorator'. Decorator ADDS behavior; Proxy CONTROLS access."
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "layers": [
        {
          "label": "Client",
          "items": [
            "Calls Subject"
          ]
        },
        {
          "label": "Proxy",
          "items": [
            "Access control",
            "Caching / Logging"
          ]
        },
        {
          "label": "Real Subject",
          "items": [
            "Actual object"
          ]
        }
      ],
      "steps": [
        {
          "active_layer": -1,
          "annotation": "System overview — step through to trace the flow."
        },
        {
          "active_layer": 0,
          "annotation": "Client holds a reference to the proxy, not the real subject."
        },
        {
          "active_layer": 1,
          "annotation": "Proxy intercepts the call — applies caching, access control, or logging."
        },
        {
          "active_layer": 2,
          "annotation": "Proxy forwards to real subject only when necessary."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the type of proxy used to delay the creation of a 'heavy' object until it is first used.",
    "starter_code": "type = \"____ Proxy\"",
    "solution": "Lazy",
    "hints": [
      "Opposite of 'eager'"
    ],
    "test_code": "def check(ans): return \"lazy\" in ans.lower()\nassert check(\"Lazy\")\nprint(\"PASS\")"
  },
  "related": [
    "decorator-pattern-01",
    "adapter-pattern-01",
    "facade-pattern-01"
  ],
  "tags": [
    "design-patterns",
    "structural",
    "proxy",
    "lazy-loading",
    "security",
    "software-engineering"
  ]
}