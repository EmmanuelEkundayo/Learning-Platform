{
  "id": "react-testing-library-01",
  "slug": "react-testing-library",
  "title": "React Testing Library (RTL)",
  "domain": "Frontend",
  "category": "Testing & Accessibility",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A library for testing React components in a way that resembles how an actual user would interact with them, focusing on the DOM rather than component internals.",
    "analogy": "A secret shopper: the shopper doesn't care how the kitchen is organized or what the chef's name is (component state/props). They only care if the food is served correctly and the waiter is polite (the resulting DOM and events).",
    "time_complexity": "O(Q) queries run in test",
    "space_complexity": "O(D) DOM nodes",
    "when_to_use": [
      "Testing if a button click opens a modal",
      "Ensuring form validation messages appear correctly",
      "Verifying that data fetched from an API is displayed on the screen"
    ],
    "gotchas": [
      "Avoid searching by 'Test IDs' if possible. Instead, use 'Accessible Queries' (like `getByRole('button')`) to ensure your site is also accessible",
      "Do NOT test internal component state (`wrapper.state()`); RTL is designed to discourage this practice"
    ]
  },
  "visualization": {
    "type": "timeline-step",
    "config": {
      "steps": [
        {
          "label": "Render",
          "sub": "render(<Component>)",
          "annotation": "Component rendered into jsdom — realistic DOM environment."
        },
        {
          "label": "Query",
          "sub": "getByRole / Text",
          "annotation": "Query by accessibility role, label, or text — as a user would find it."
        },
        {
          "label": "Interact",
          "sub": "userEvent.click",
          "annotation": "Simulate user interactions: click, type, keyboard."
        },
        {
          "label": "Assert",
          "sub": "expect().toBeInDoc",
          "annotation": "Assert on DOM state — visible text, ARIA state, form values."
        },
        {
          "label": "Clean up",
          "sub": "afterEach",
          "annotation": "DOM and state cleaned up automatically after each test."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the most recommended query type in RTL because it mimics how screen readers find elements.",
    "starter_code": "test('loads and displays greeting', async () => {\n    render(<Greeting />)\n    # Prefer this over getByTestId or querySelector\n    const button = screen._________('button', { name: /login/i })\n    # ...\n})",
    "solution": "getByRole",
    "hints": [
      "It gets an element by its ARIA 'Role'"
    ],
    "test_code": "def check(ans): return \"role\" in ans.lower()\nassert check(\"getByRole\")\nprint(\"PASS\")"
  },
  "related": [
    "unit-vs-integration-e2e-01",
    "aria-roles-01",
    "mocking-strategies-frontend-01"
  ],
  "tags": [
    "testing",
    "react",
    "rtl",
    "accessibility",
    "dom",
    "frontend"
  ]
}