{
  "id": "secrets-management-01",
  "slug": "secrets-management",
  "title": "Secrets Management",
  "domain": "Backend",
  "category": "Security",
  "difficulty": "intermediate",
  "card": {
    "intuition": "The practice of securely storing and managing sensitive information like API keys, database passwords, and private certificates, rather than hardcoding them in your code.",
    "analogy": "A safe in a hotel room: instead of carrying your passport and diamonds in your pocket (code) where anyone can see them, you put them in a secure box and only use the 'Key Code' (Environment Variable) to access them.",
    "time_complexity": "O(1) per secret lookup",
    "space_complexity": "O(S) secrets stored",
    "when_to_use": [
      "Managing database credentials across Development, Staging, and Production",
      "Storing API keys for 3rd party services (Stripe, Twilio, OpenAI)",
      "Automating the 'Rotation' of keys (changing them every 30 days) for better security"
    ],
    "gotchas": [
      "Never commit your `.env` file to GitHub! If you do, your keys are public and hackers WILL find them within seconds",
      "Storing secrets in 'Environment Variables' is a good step, but using a dedicated Secrets Manager (like AWS Secrets Manager or HashiCorp Vault) is far more secure for large teams"
    ]
  },
  "visualization": {
    "type": "architecture-diagram",
    "config": {
      "steps": [
        {
          "label": "Dev",
          "sub": ".env (local only)",
          "annotation": "Development: .env file, never committed. gitignored."
        },
        {
          "label": "CI",
          "sub": "Injected at build",
          "annotation": "CI: secrets injected as env vars from CI secret store. Not in code."
        },
        {
          "label": "Production",
          "sub": "Secrets manager",
          "annotation": "Production: app fetches secrets from Vault / AWS Secrets Manager at runtime."
        },
        {
          "label": "Rotate",
          "sub": "Zero-downtime",
          "annotation": "Rotation: update secret in manager. App picks up on next fetch — no redeploy."
        },
        {
          "label": "Audit",
          "sub": "Access log",
          "annotation": "All secret access logged — who accessed what and when."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the file that should ALWAYS be added to .gitignore.",
    "starter_code": "def get_ignored_files():\n    return [\".DS_Store\", \"node_modules/\", \"_____ file\"]\n",
    "solution": ".env",
    "hints": [
      "It's an 'environmental' file"
    ],
    "test_code": "assert \".env\" in get_ignored_files()[2]\nprint(\"PASS\")"
  },
  "related": [
    "password-hashing-01",
    "api-gateway-01",
    "load-balancing-01"
  ],
  "tags": [
    "security",
    "secrets",
    "env",
    "vault",
    "devops",
    "backend"
  ]
}