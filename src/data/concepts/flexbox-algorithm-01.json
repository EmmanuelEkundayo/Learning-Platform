{
  "id": "flexbox-algorithm-01",
  "slug": "flexbox-algorithm",
  "title": "Flexbox Algorithm",
  "domain": "Frontend",
  "category": "CSS & Layout",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A one-dimensional layout model that distributes space among items in a container, allowing them to shrink or grow to fill available space along a main axis.",
    "analogy": "A line of people at a bus stop: the bus stop (Flex Container) can tell people to stand in a row (row) or a column (column). If there is extra room, people can spread out (justify-content), and if it's too crowded, they can squeeze together (flex-shrink).",
    "time_complexity": "O(N) for layout calculation",
    "space_complexity": "O(N) flex items",
    "when_to_use": [
      "Aligning items in a navigation bar",
      "Centering content vertically and horizontally",
      "Creating simple, responsive one-dimensional rows or columns"
    ],
    "gotchas": [
      "The 'Main Axis' changes depending on `flex-direction`. If direction is `column`, `justify-content` controls vertical alignment, not horizontal",
      "Flex items by default have `min-width: auto`, which can sometimes prevent them from shrinking as much as you expect"
    ]
  },
  "visualization": {
    "type": "matrix-grid",
    "config": {
      "steps": [
        {
          "label": "Container",
          "sub": "display: flex",
          "annotation": "Flex container established. Children become flex items."
        },
        {
          "label": "Main Axis",
          "sub": "flex-direction",
          "annotation": "Main axis set by flex-direction: row (default) or column."
        },
        {
          "label": "Flexibility",
          "sub": "flex-grow/shrink",
          "annotation": "Available space distributed: flex-grow expands, flex-shrink contracts."
        },
        {
          "label": "Alignment",
          "sub": "justify/align",
          "annotation": "justify-content aligns on main axis. align-items on cross axis."
        },
        {
          "label": "Wrapping",
          "sub": "flex-wrap",
          "annotation": "flex-wrap: wrap allows items to flow to new lines when needed."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Identify the CSS property used to align flex items along the CROSS axis (perpendicular to the main axis).",
    "starter_code": ".container {\n    display: flex;\n    flex-direction: row;\n    # Align items vertically (cross axis)\n    ______-items: center;\n}",
    "solution": "align",
    "hints": [
      "It starts with 'a' and rhymes with 'assign'"
    ],
    "test_code": "def check(ans): return ans.lower() == \"align\"\nassert check(\"align\")\nprint(\"PASS\")"
  },
  "related": [
    "css-grid-01",
    "responsive-design-01",
    "stacking-context-01"
  ],
  "tags": [
    "css",
    "layout",
    "flexbox",
    "alignment",
    "responsive",
    "frontend"
  ]
}