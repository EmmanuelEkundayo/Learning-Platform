{
  "id": "dependency-injection-01",
  "slug": "dependency-injection",
  "title": "Dependency Injection (DI)",
  "domain": "Backend",
  "category": "APIs & Architecture",
  "difficulty": "intermediate",
  "card": {
    "intuition": "A design pattern where an object's dependencies are provided ('injected') from the outside, rather than the object creating them itself.",
    "analogy": "Building a Lego set: instead of the Lego house having to build its own front door and windows, you (the builder) just snap existing doors and windows (dependencies) onto the house.",
    "time_complexity": "O(G) dependency graph resolution",
    "space_complexity": "O(G) dependency graph",
    "when_to_use": [
      "Writing unit tests (inject a 'MockDatabase' instead of a real one)",
      "Keeping your code decoupled and easy to refactor",
      "Using frameworks like Spring (Java), NestJS (TS), or Angular"
    ],
    "gotchas": [
      "Don't confuse DI with a 'Service Locator' — DI is about receiving dependencies, Service Locator is about asking for them",
      "In simple scripts, over-using DI can add unnecessary complexity 'boilerplate' that makes the code harder to read"
    ]
  },
  "visualization": {
    "type": "architecture-diagram",
    "config": {
      "layers": [
        {
          "label": "DI Container",
          "items": [
            "Bindings",
            "Lifecycle"
          ]
        },
        {
          "label": "Injected Into",
          "items": [
            "Controller",
            "Service"
          ]
        },
        {
          "label": "Dependencies",
          "items": [
            "DB Repo",
            "Email Svc",
            "Logger"
          ]
        }
      ],
      "steps": [
        {
          "active_layer": -1,
          "annotation": "System overview — step through to trace the flow."
        },
        {
          "active_layer": 0,
          "annotation": "DI container holds bindings: interface → implementation."
        },
        {
          "active_layer": 1,
          "annotation": "Container injects resolved dependencies into consumer constructors."
        },
        {
          "active_layer": 2,
          "annotation": "Dependencies are concrete implementations — swappable for tests."
        }
      ]
    }
  },
  "exercise": {
    "type": "fill-in-the-blank",
    "prompt": "Convert this tightly coupled code into Dependency Injection style.",
    "starter_code": "# Before (Tight Coupling)\nclass UserService:\n    def __init__(self):\n        self.db = Database() # Locked in!\n\n# After (Dependency Injection)\nclass UserService:\n    def __init__(self, _______):\n        self.db = _______",
    "solution": "db\ndb",
    "hints": [
      "Pass the 'db' instance into the constructor instead of creating it inside"
    ],
    "test_code": "class MockDB: pass\nus = UserService(MockDB())\nassert isinstance(us.db, MockDB)\nprint(\"PASS\")"
  },
  "related": [
    "repository-pattern-01",
    "middleware-patterns-01",
    "orm-patterns-01"
  ],
  "tags": [
    "dependency-injection",
    "di",
    "ioc",
    "testing",
    "design-patterns",
    "backend"
  ]
}